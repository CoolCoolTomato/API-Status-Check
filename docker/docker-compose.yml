services:
  app:
    image: coolcooltomato/api-status-check:latest
    container_name: api-status-check
    restart: always
    ports:
      - "8080:8080"
    environment:
      ADMIN_KEY: ${ADMIN_KEY:-changeme}
    volumes:
      - ./data:/app/data